def plus_one(digits: list[int]) -> list[int]:
    for i in range(0,len(digits),-1):
        if digits[i] != 9:
            digits[i] += 1
        else:
            digits[i] = 0
    return digits
            
    # if [9]*(len(digits)) == digits:
    #     result = [0]*(len(digits)+1)
    #     result[0] = 1
    # return result
